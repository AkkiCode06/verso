name: Wavelength
options:
  bundleIdPrefix: com.akki
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true

targets:
  Wavelength:
    type: application
    platform: macOS
    sources:
      - path: Sources
    info:
      path: Sources/Info.plist
      properties:
        CFBundleDisplayName: Wavelength
        CFBundleShortVersionString: 0.1.0
        CFBundleVersion: "1"
        LSUIElement: true
        NSHumanReadableCopyright: © 2026
        NSAppleEventsUsageDescription: Wavelength reads the currently playing track and its playback position from Music, so it can show lyrics in time with what you are listening to. Nothing leaves your Mac.
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.akki.wavelength
        SWIFT_VERSION: "5.0"
        CODE_SIGN_IDENTITY: "-"
        CODE_SIGN_STYLE: Manual
        DEVELOPMENT_TEAM: ""
        ENABLE_HARDENED_RUNTIME: false
        COMBINE_HIDPI_IMAGES: true
        MACOSX_DEPLOYMENT_TARGET: "14.0"
